query GetAllCat($input: InputStructure) {
  getAllCat(Input: $input) {
    status
    count
    cats {
      firstName
      id
    }
    cats {
      lastName
      id
      firstName
      age
    }
    errors {
      field
      message
    }
  }
}
