import { toBoolean } from "../../utils/stories/toBoolean";

export const tableArgsTypes = {
  isLoading: toBoolean(),
  isEmpty: toBoolean(),
  isCompact: toBoolean(),
  hasFooter: toBoolean(),
  isZebra: toBoolean(),
};
